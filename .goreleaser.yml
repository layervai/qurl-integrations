version: 2

project_name: qurl

before:
  hooks:
    - go mod tidy
    # Generate man pages for inclusion in archives
    - go build -o /tmp/qurl-gendocs ./apps/cli/cmd/
    - /tmp/qurl-gendocs docs man -d manpages

builds:
  - id: qurl
    main: ./apps/cli/cmd/
    binary: qurl
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w -X main.version={{.Version}}
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64

archives:
  - id: qurl
    builds:
      - qurl
    name_template: "qurl_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    files:
      - LICENSE
      - manpages/*
    format_overrides:
      - goos: windows
        format: zip

checksum:
  name_template: "checksums.txt"

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^ci:"
      - "^chore:"

brews:
  - name: qurl
    repository:
      owner: layervai
      name: homebrew-tap
      token: "{{ .Env.HOMEBREW_TAP_GITHUB_TOKEN }}"
    directory: Formula
    homepage: "https://layerv.ai"
    description: "qURL CLI - manage secure links from the command line"
    license: "MIT"
    install: |
      bin.install "qurl"

      # Shell completions
      generate_completions_from_executable(bin/"qurl", "completion")

      # Man pages (included in archive by goreleaser)
      man1.install Dir["manpages/*.1"] if Dir.exist?("manpages")
    test: |
      assert_match version.to_s, shell_output("#{bin}/qurl version")

nfpms:
  - id: qurl
    builds:
      - qurl
    vendor: LayerV
    homepage: "https://layerv.ai"
    maintainer: "LayerV <support@layerv.ai>"
    description: "qURL CLI - manage secure links from the command line"
    license: "MIT"
    formats:
      - deb
      - rpm
    contents:
      - src: manpages/qurl.1
        dst: /usr/share/man/man1/qurl.1

release:
  # release-please creates the GitHub Release (tag + changelog notes) before
  # GoReleaser runs (see .github/workflows/release-please.yml); GoReleaser
  # must attach artifacts to it without replacing those notes. keep-existing
  # is the current default — pinned because the handshake breaks if the
  # default ever changes.
  mode: keep-existing
  # Make re-runs idempotent: without this, re-running the release-cli job
  # after a partial upload 422s on the assets that did make it.
  replace_existing_artifacts: true
