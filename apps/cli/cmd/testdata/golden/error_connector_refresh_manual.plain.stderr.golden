Error: This Connector needs its qURL platform assignment refreshed, and that refresh waits for your explicit approval.

  native assignment refresh requires explicit operator approval (sustained connection failures): inspect the failure, then approve exactly one start with refresh mode auto; an orchestrator restart is not approval

  Hint: review why it stopped (see your previous run's output), then run the same command once with --refresh-mode auto. That flag approves only this start; automatic restarts are deliberately not treated as approval.
