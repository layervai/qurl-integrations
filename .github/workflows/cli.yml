name: "cli: Build and Test"

# Path filtering lives in the `changes` job below, NOT in `on: paths:`.
# A workflow skipped by a path filter never reports its checks, so a
# `cli / required` branch-protection rule would block every PR that
# doesn't touch apps/cli/. The detector + always-running aggregate keeps
# the check reporting on every PR — matching slack/discord/chrome-extension/shared.
on:
  push:
    branches: [main]
  pull_request:
    # Stacked CLI PRs must run the same gates before their base reaches main.
    branches: ["**"]

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}

jobs:
  changes:
    name: cli / detect changes
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions:
      contents: read
      pull-requests: read
      # Reading this workflow's own run history is what lets the step below
      # find the last SHA it actually validated.
      actions: read
    outputs:
      # An unresolvable base forces every gate to run. A redundant run costs
      # minutes; a wrongly narrow diff ships unvalidated code green (#1022).
      cli: ${{ steps.base.outputs.force == 'true' && 'true' || steps.filter.outputs.cli }}
    steps:
      # On PRs, paths-filter uses the pull request API; push events need git
      # history to compute the diff.
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        if: github.event_name == 'push'
        with:
          fetch-depth: 0

      # `cancel-in-progress: false` protects a run already in progress, not one
      # still pending: GitHub keeps at most one pending run per concurrency
      # group, so a burst of merges to main cancels the intermediate runs.
      # Diffing this push alone would then skip the gates for changes that
      # landed in those cancelled runs — every check green, app CI never having
      # executed (#1022). Diff against the last validated SHA instead.
      - name: Resolve validated base
        id: base
        if: github.event_name == 'push'
        env:
          GH_TOKEN: ${{ github.token }}
        run: scripts/resolve-validated-base.sh

      - uses: dorny/paths-filter@ceb8a2b8f2d89434be7ff52d3de7ec3738c5cc9d # v4.0.3
        id: filter
        # Skipped when no base could be resolved. The job output already forces
        # every gate true there, and running the filter anyway would let a
        # filter error redden this job instead of honouring the fail-closed
        # intent.
        if: steps.base.outputs.force != 'true'
        with:
          # Empty on pull_request, where the push-only step above never ran and
          # paths-filter takes its PR API path instead.
          base: ${{ steps.base.outputs.sha }}
          filters: |
            cli:
              # Source of truth for CLI-impacting paths on PR and main push events.
              # No 'shared/**' here: the v2 CLI does not import shared/ at all, and
              # even if an import returns, shared-test.yml's `shared / test all` gate
              # runs `go test ./...` — apps/cli included — on every shared/ change,
              # so those changes are validated there.
              # .goreleaser.yml is here because the goreleaser-check gate below is
              # the only CI that validates it before release-cli consumes it.
              # The callback path is intentionally CLI-impacting: any trust or
              # schema change must mint a fresh artifact and pass the complete
              # exact-artifact journey, not only the generic scripts workflow.
              - 'apps/cli/**'
              - 'go.mod'
              - 'go.sum'
              - '.golangci.yml'
              - '.goreleaser.yml'
              - '.github/workflows/cli.yml'
              - '.github/workflows/qurl-cli-customer-journey-result.yml'
              - '.github/workflows/release-please.yml'
              - 'scripts/accept-cli-customer-journey-result.sh'
              - 'scripts/build-sandbox-matched-cohort-pr-artifacts.sh'
              - 'scripts/build-sandbox-matched-cohort-pr-artifacts_test.sh'
              - 'scripts/resolve-sandbox-artifact-qurl-go-source.sh'
              - 'scripts/resolve-sandbox-artifact-qurl-go-source_test.sh'
              - 'scripts/extract-qurl-image-attestations.sh'
              - 'scripts/test-accept-cli-customer-journey-result.sh'

  lint:
    name: cli / lint
    runs-on: ubuntu-latest
    timeout-minutes: 20
    needs: changes
    if: needs.changes.outputs.cli == 'true'
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version-file: go.mod
          cache: true

      - name: Verify golangci-lint config
        run: go run github.com/golangci/golangci-lint/v2/cmd/golangci-lint@v2.12.2 config verify

      - name: golangci-lint
        uses: golangci/golangci-lint-action@ba0d7d2ec06a0ea1cb5fa41b2e4a3ab91d21278a # v9.3.0
        with:
          version: v2.12.2
          verify: false # Avoid the action's network JSONSchema fetch; config verification runs in the previous pinned step.
          args: --timeout=5m ./apps/cli/...

      - name: Cross-lint Windows CLI sources
        run: |
          command -v golangci-lint
          GOOS=windows GOARCH=amd64 CGO_ENABLED=0 golangci-lint run --timeout=5m ./apps/cli/...

      - name: golangci-lint sandbox tests
        run: >-
          go run github.com/golangci/golangci-lint/v2/cmd/golangci-lint@v2.12.2 run
          --build-tags=clisandbox,clisoak --timeout=5m ./apps/cli/...

  test:
    name: cli / test
    runs-on: ubuntu-latest
    timeout-minutes: 30 # This job compiles separate race, coverage, and tagged CLI test variants.
    needs: changes
    if: needs.changes.outputs.cli == 'true'
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version-file: go.mod
          cache: true

      - name: Run tests with coverage
        run: |
          go test -race -count=1 -coverprofile=coverage.out -covermode=atomic \
            ./apps/cli/...

      # This is an independent named release gate, not an accidental repeat of
      # the coverage run. The private lifecycle receipt binds this exact step
      # and test set, so keep their result visible and stable on every CLI PR.
      - name: Run CRID lifecycle unit tests
        shell: bash
        env:
          LIFECYCLE_TEST_REGEX: '^(TestDaemonServesTwoResourcesAndStopsOneIndependently|TestLocalPublishCompensatesSetupFailureBeforeDaemonOwnership|TestPublishDaemonLifecycleServesRealHTTPAndStopsCleanly|TestPublishNewMachineTakeoverRotatesEpochOnce|TestRestartReconcilesAmbiguousAppliedPostWithoutReplay|TestRestartSetupFailureAlwaysCompensatesAdvancedEpoch|TestShareLifecycleCommandsConvergeCloudRegistryAndDaemon|TestStartFailsImmediatelyWhenServingEpochAdvances|TestStartRotatesEpochAfterLocalTerminalDisable)$'
          LIFECYCLE_TEST_NAMES: |-
            TestDaemonServesTwoResourcesAndStopsOneIndependently
            TestLocalPublishCompensatesSetupFailureBeforeDaemonOwnership
            TestPublishDaemonLifecycleServesRealHTTPAndStopsCleanly
            TestPublishNewMachineTakeoverRotatesEpochOnce
            TestRestartReconcilesAmbiguousAppliedPostWithoutReplay
            TestRestartSetupFailureAlwaysCompensatesAdvancedEpoch
            TestShareLifecycleCommandsConvergeCloudRegistryAndDaemon
            TestStartFailsImmediatelyWhenServingEpochAdvances
            TestStartRotatesEpochAfterLocalTerminalDisable
        run: |
          set -euo pipefail
          actual_tests="$(
            go test -race -list "$LIFECYCLE_TEST_REGEX" ./apps/cli/cmd |
              sed -n '/^Test/p' |
              LC_ALL=C sort
          )"
          if [[ "$actual_tests" != "$LIFECYCLE_TEST_NAMES" ]]; then
            echo "::error::CRID lifecycle test declarations differ from the required set"
            diff -u \
              <(printf '%s\n' "$LIFECYCLE_TEST_NAMES") \
              <(printf '%s\n' "$actual_tests")
            exit 1
          fi
          lifecycle_events=$(mktemp)
          trap 'rm -f "$lifecycle_events"' EXIT
          lifecycle_status=0
          go test -race -count=1 -json -run "$LIFECYCLE_TEST_REGEX" ./apps/cli/cmd \
            >"$lifecycle_events" || lifecycle_status=$?
          jq -j 'select(.Output != null) | .Output' "$lifecycle_events"
          passed_tests="$(
            jq -r --arg regex "$LIFECYCLE_TEST_REGEX" \
              'select(.Action == "pass" and ((.Test // "") | test($regex))) | .Test' \
              "$lifecycle_events" |
              LC_ALL=C sort
          )"
          skipped_tests="$(
            jq -r --arg regex "$LIFECYCLE_TEST_REGEX" \
              'select(.Action == "skip" and ((.Test // "") | test($regex))) | .Test' \
              "$lifecycle_events" |
              LC_ALL=C sort
          )"
          if [[ -n "$skipped_tests" ]]; then
            echo "::error::required CRID lifecycle tests skipped"
            printf '%s\n' "$skipped_tests"
            exit 1
          fi
          if [[ "$passed_tests" != "$LIFECYCLE_TEST_NAMES" ]]; then
            echo "::error::required CRID lifecycle tests did not all report PASS"
            diff -u \
              <(printf '%s\n' "$LIFECYCLE_TEST_NAMES") \
              <(printf '%s\n' "$passed_tests")
            exit 1
          fi
          if (( lifecycle_status != 0 )); then
            echo "::error::CRID lifecycle test process failed with status $lifecycle_status"
            exit "$lifecycle_status"
          fi

      # 40 is the intentional initial floor, not the target. The v2 rebuild
      # replaces this tree, so pinning an ambition floor (85) to the old tree
      # would be red on day one for coverage the rebuild is about to delete.
      # Raise this — and the mirrored floor in Makefile `check-cli` — once the
      # v2 code lands. The pre-v2 tree measured 71.9% when this gate shipped.
      - name: Check coverage threshold
        run: |
          COVERAGE=$(go tool cover -func=coverage.out | grep ^total: | awk '{print $3}' | tr -d '%')
          echo "Total coverage: ${COVERAGE}%"
          if ! awk -v c="$COVERAGE" 'BEGIN { exit !(c+0 >= 40) }' </dev/null; then
            echo "::error::Coverage ${COVERAGE}% is below 40% threshold"
            exit 1
          fi

      - name: Vet
        run: go vet ./apps/cli/...

      # Credentialed sandbox journeys run in the private orchestrator. Public
      # CI still compiles every tagged test so API and platform drift fail here.
      - name: Compile private sandbox test surface
        run: go test -race -tags=clisandbox -run '^$' -count=1 ./apps/cli/...

      # These tests need no service credentials or live infrastructure. They
      # execute the exact-artifact process and lifecycle validation helpers on
      # the Ubuntu runner where their POSIX fixture is supported.
      - name: Run credential-free sandbox validator tests
        shell: bash
        env:
          VALIDATOR_TEST_REGEX: '^(TestReadSandboxSecretFileFailsClosed|TestRunSandboxLocalCLIForwardsOnlyHardenedImageBinding|TestRunSandboxLocalCLIUsesExactBinaryAndState|TestSandboxForegroundLifecycleStateContract|TestSandboxHarnessPassesInlineAPIKeyToExactBinary|TestSandboxNamespaceIsCanonicalAndSeparated|TestSandboxProcessRecoveryCleanupAfterPreReadyFailure|TestSandboxPublishProcessReportsEarlyExit|TestSandboxPublishReadinessWaitsForCompleteCRIDLine|TestSandboxRunIdentityBindsOnlyImmutableHardenedImage|TestSandboxSiblingCleanupPreservesDeviceAfterResourceFailure|TestSandboxStoppedRouteRefusalMatchesQuietGet|TestValidateSandboxCLIBinary|TestValidateSandboxDeviceIdentity|TestValidateSandboxRouteFence|TestValidateSandboxSharingTransitionRequiresAdvancedEpoch)$'
          VALIDATOR_TEST_NAMES: |-
            TestReadSandboxSecretFileFailsClosed
            TestRunSandboxLocalCLIForwardsOnlyHardenedImageBinding
            TestRunSandboxLocalCLIUsesExactBinaryAndState
            TestSandboxForegroundLifecycleStateContract
            TestSandboxHarnessPassesInlineAPIKeyToExactBinary
            TestSandboxNamespaceIsCanonicalAndSeparated
            TestSandboxProcessRecoveryCleanupAfterPreReadyFailure
            TestSandboxPublishProcessReportsEarlyExit
            TestSandboxPublishReadinessWaitsForCompleteCRIDLine
            TestSandboxRunIdentityBindsOnlyImmutableHardenedImage
            TestSandboxSiblingCleanupPreservesDeviceAfterResourceFailure
            TestSandboxStoppedRouteRefusalMatchesQuietGet
            TestValidateSandboxCLIBinary
            TestValidateSandboxDeviceIdentity
            TestValidateSandboxRouteFence
            TestValidateSandboxSharingTransitionRequiresAdvancedEpoch
        run: |
          set -euo pipefail
          actual_tests="$(
            go test -race -tags=clisandbox -list "$VALIDATOR_TEST_REGEX" ./apps/cli/cmd |
              sed -n '/^Test/p' |
              LC_ALL=C sort
          )"
          if [[ "$actual_tests" != "$VALIDATOR_TEST_NAMES" ]]; then
            echo "::error::credential-free validator declarations differ from the required set"
            diff -u <(printf '%s\n' "$VALIDATOR_TEST_NAMES") <(printf '%s\n' "$actual_tests")
            exit 1
          fi
          events=$(mktemp)
          trap 'rm -f "$events"' EXIT
          validator_status=0
          go test -race -tags=clisandbox -count=1 -json \
            -run "$VALIDATOR_TEST_REGEX" ./apps/cli/cmd >"$events" || validator_status=$?
          jq -j 'select(.Output != null) | .Output' "$events"
          passed="$(jq -r --arg regex "$VALIDATOR_TEST_REGEX" \
            'select(.Action == "pass" and ((.Test // "") | test($regex))) | .Test' "$events" | LC_ALL=C sort)"
          skipped="$(jq -r --arg regex "$VALIDATOR_TEST_REGEX" \
            'select(.Action == "skip" and ((.Test // "") | test($regex))) | .Test' "$events" | LC_ALL=C sort)"
          if [[ -n "$skipped" ]]; then
            echo "::error::required credential-free validators skipped"
            printf '%s\n' "$skipped"
            exit 1
          fi
          if [[ "$passed" != "$VALIDATOR_TEST_NAMES" ]]; then
            echo "::error::required credential-free validators did not all report PASS"
            diff -u <(printf '%s\n' "$VALIDATOR_TEST_NAMES") <(printf '%s\n' "$passed")
            exit 1
          fi
          if (( validator_status != 0 )); then
            echo "::error::credential-free validator test process failed with status $validator_status"
            exit "$validator_status"
          fi

      - name: Run exact warm-daemon process contract
        shell: bash
        env:
          WARM_DAEMON_TEST_REGEX: '^TestExactWarmDaemonProcessContract$'
          WARM_DAEMON_TEST_NAME: TestExactWarmDaemonProcessContract
        run: |
          set -euo pipefail
          actual_test="$(go test -race -tags='clisandbox clisoak' -list "$WARM_DAEMON_TEST_REGEX" ./apps/cli/cmd | sed -n '/^Test/p')"
          [[ "$actual_test" == "$WARM_DAEMON_TEST_NAME" ]] || {
            echo "::error::exact warm-daemon declaration is missing or ambiguous"
            exit 1
          }
          events=$(mktemp)
          trap 'rm -f "$events"' EXIT
          warm_daemon_status=0
          go test -race -tags='clisandbox clisoak' -count=1 -json \
            -run "$WARM_DAEMON_TEST_REGEX" ./apps/cli/cmd >"$events" || warm_daemon_status=$?
          jq -j 'select(.Output != null) | .Output' "$events"
          passed="$(jq -r --arg regex "$WARM_DAEMON_TEST_REGEX" \
            'select(.Action == "pass" and ((.Test // "") | test($regex))) | .Test' "$events")"
          skipped="$(jq -r --arg regex "$WARM_DAEMON_TEST_REGEX" \
            'select(.Action == "skip" and ((.Test // "") | test($regex))) | .Test' "$events")"
          if [[ -n "$skipped" ]]; then
            echo "::error::required exact warm-daemon test skipped"
            printf '%s\n' "$skipped"
            exit 1
          fi
          if [[ "$passed" != "$WARM_DAEMON_TEST_NAME" ]]; then
            echo "::error::exact warm-daemon contract did not report one PASS"
            diff -u <(printf '%s\n' "$WARM_DAEMON_TEST_NAME") <(printf '%s\n' "$passed")
            exit 1
          fi
          if (( warm_daemon_status != 0 )); then
            echo "::error::exact warm-daemon test process failed with status $warm_daemon_status"
            exit "$warm_daemon_status"
          fi

      - name: Test sandbox matched-cohort artifact contract
        shell: bash
        run: |
          shellcheck \
            scripts/build-sandbox-matched-cohort-pr-artifacts.sh \
            scripts/build-sandbox-matched-cohort-pr-artifacts_test.sh \
            scripts/resolve-sandbox-artifact-qurl-go-source.sh \
            scripts/resolve-sandbox-artifact-qurl-go-source_test.sh
          scripts/build-sandbox-matched-cohort-pr-artifacts_test.sh
          scripts/resolve-sandbox-artifact-qurl-go-source_test.sh

  govulncheck:
    name: cli / govulncheck
    runs-on: ubuntu-latest
    timeout-minutes: 20
    needs: changes
    if: needs.changes.outputs.cli == 'true'
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version-file: go.mod
          cache: true

      - name: Run govulncheck
        run: go tool govulncheck ./apps/cli/...

  matrix:
    # Static display name on purpose — never interpolate ${{ matrix.os }} into
    # it. The ciworkflows contract conscripts quality gates by this literal
    # name, and the required aggregate's verifier renders it; an interpolated
    # name would register three per-OS check contexts that neither the
    # aggregate's needs set nor branch protection can enumerate.
    name: cli / matrix
    runs-on: ${{ matrix.os }}
    needs: changes
    if: needs.changes.outputs.cli == 'true'
    timeout-minutes: 25
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    env:
      # The v2 suite uses the real platform filesystem and keyring surfaces on
      # each hosted OS. No customer credential or external service is used.
      QURL_TEST_HARNESS: "1"
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version-file: go.mod
          cache: true

      - name: Scan Windows CLI dependencies
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          go tool govulncheck ./apps/cli/...
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }

      # The live credential-store smoke needs an unlocked keyring on every OS.
      # CI keyrings start locked on macOS and absent on Linux; Windows
      # Credential Manager works headless as-is.
      - name: Set up macOS keychain
        if: runner.os == 'macOS'
        shell: bash
        run: |
          # Ephemeral CI keychain: throwaway random password, auto-lock timeout
          # cleared, made the default so keyring libraries find it unlocked.
          password="$(uuidgen)"
          security create-keychain -p "$password" qurl-ci.keychain
          security set-keychain-settings qurl-ci.keychain
          security default-keychain -s qurl-ci.keychain
          security unlock-keychain -p "$password" qurl-ci.keychain

      - name: Set up Linux keyring (gnome-keyring over D-Bus)
        if: runner.os == 'Linux'
        shell: bash
        timeout-minutes: 5
        run: |
          select_fallback_mirrors() {
            local status
            echo "::warning::Switching Linux package downloads away from the slow Azure mirror"
            # TODO(upstream-contract): actions/runner-images routes apt through
            # /etc/apt/apt-mirrors.txt and configure-apt-sources.sh writes one
            # URI plus a tab-delimited priority on each mirror-list line.
            if printf '%s\tpriority:%s\n' \
              'https://archive.ubuntu.com/ubuntu/' '1' \
              'https://security.ubuntu.com/ubuntu/' '2' | \
              sudo -n tee /etc/apt/apt-mirrors.txt >/dev/null; then
              return 0
            else
              status=$?
            fi
            echo "::error::Unable to select the bounded Ubuntu package mirror fallback"
            return "$status"
          }

          install_keyring_packages() {
            local attempt status
            for attempt in 1 2; do
              echo "Installing Linux keyring packages (attempt ${attempt}/2)"
              if sudo -n env DEBIAN_FRONTEND=noninteractive \
                timeout --signal=TERM --kill-after=10s 90s \
                apt-get \
                  -o Acquire::Retries=2 \
                  -o Acquire::http::Timeout=20 \
                  -o Acquire::https::Timeout=20 \
                  -o DPkg::Lock::Timeout=30 \
                  -o Dpkg::Use-Pty=0 \
                  install -y --no-install-recommends gnome-keyring dbus; then
                return 0
              else
                status=$?
              fi
              echo "::warning::Linux keyring package install attempt ${attempt}/2 failed with exit ${status}"
              if [ "$attempt" -lt 2 ]; then
                # runner-images already routes apt through this mirror list;
                # the TODO(upstream-contract) above tracks that dependency.
                # A slow-but-responsive Azure endpoint does not trigger apt's
                # own failover, so make the official Ubuntu mirrors explicit
                # before the one remaining bounded attempt.
                if ! select_fallback_mirrors; then
                  return 1
                fi
                sleep 5
              fi
            done
            echo "::error::Linux keyring package install failed after 2 bounded attempts"
            return "$status"
          }

          # Hosted images already carry D-Bus and may eventually carry
          # gnome-keyring too. Avoid an unnecessary apt index refresh: the
          # image's configured Azure mirror can stall indefinitely while
          # fetching package indexes or archives. The image's package metadata
          # is sufficient for installing its own release packages, and a slow
          # first download gets one retry through the official Ubuntu mirrors.
          if ! command -v gnome-keyring-daemon >/dev/null 2>&1 || ! command -v dbus-run-session >/dev/null 2>&1; then
            install_keyring_packages
          fi
          for command in gnome-keyring-daemon dbus-run-session; do
            if ! command -v "$command" >/dev/null 2>&1; then
              echo "::error::Linux keyring setup completed without required command: ${command}"
              exit 1
            fi
          done
          # gnome-keyring-daemon cannot create/unlock the default collection
          # unless the keyrings directory already exists with owner-only
          # permissions. This preparation (and the unlock in the test step)
          # is zalando/go-keyring's own CI recipe — the upstream-proven shape.
          mkdir -p "$HOME/.cache/"
          mkdir -p "$HOME/.local/share/keyrings/"
          chmod -R 700 "$HOME/.local/"
          # Arms the dbus-run-session wrapper in the test step below: the
          # daemon must be unlocked inside the same session the tests run in,
          # so the wrapper cannot live in this step.
          echo "QURL_CLI_KEYRING_DBUS=1" >> "$GITHUB_ENV"

      - name: Build and smoke the Windows CLI executable
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $binary = Join-Path $env:RUNNER_TEMP 'qurl.exe'
          $env:GOARCH = 'amd64'
          $env:CGO_ENABLED = '0'
          go build -o $binary ./apps/cli/cmd/
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          $help = & $binary help | Out-String
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          foreach ($command in @('daemon', 'inspect', 'restart', 'start', 'status', 'stop')) {
            if ($help -notmatch "(?m)^\s+$command\s") {
              Write-Error "Windows qurl help is missing the $command command"
              exit 1
            }
          }
          & $binary version
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }

          $arm64Binary = Join-Path $env:RUNNER_TEMP 'qurl-arm64.exe'
          $env:GOARCH = 'arm64'
          go build -o $arm64Binary ./apps/cli/cmd/
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          if (-not (Test-Path -LiteralPath $arm64Binary -PathType Leaf)) {
            Write-Error 'Windows arm64 qurl build did not produce qurl-arm64.exe'
            exit 1
          }

      - name: Run pinned Windows Task Scheduler integration
        if: runner.os == 'Windows'
        shell: pwsh
        env:
          QURL_WINDOWS_USER_JOB_INTEGRATION: '1'
        run: |
          go test -count=1 github.com/layervai/qurl-connector/pkg/service
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }

      - name: Run tests
        shell: bash
        run: |
          if [ "${QURL_CLI_KEYRING_DBUS:-}" = "1" ]; then
            # The unlock password must be non-empty and piped to the daemon
            # inside the same D-Bus session the tests run in; an empty
            # password leaves the Secret Service refusing every store with
            # "failed to unlock correct collection
            # '/org/freedesktop/secrets/aliases/default'". The incantation is
            # copied from zalando/go-keyring's own CI (a throwaway password
            # for an ephemeral runner keyring, not a secret). -eo pipefail
            # makes a failed unlock redden this step itself instead of
            # surfacing as a confusing failure inside the tests.
            dbus-run-session -- bash -c "set -eo pipefail; echo 'somecredstorepass' | gnome-keyring-daemon --unlock; exec go test -count=1 ./apps/cli/..."
          else
            go test -count=1 ./apps/cli/...
          fi

  sandbox-customer-artifacts:
    name: cli / sandbox matched-cohort artifacts
    runs-on: ubuntu-latest
    needs: changes
    timeout-minutes: 15
    # NHP post-deploy consumes an artifact from the exact current main SHA.
    # Therefore every main push must produce one even when this push did not
    # change CLI paths. PRs keep the normal CLI-impacting path boundary.
    if: (github.event_name == 'push' && github.ref == 'refs/heads/main') || needs.changes.outputs.cli == 'true'
    permissions:
      contents: read
    steps:
      # Artifacts from a fork must never become executable input to the
      # protected NHP journey that consumes this producer in the next PR.
      # Keep the reporting job present and red instead of hiding it behind a
      # job-level condition.
      - name: Require the trusted artifact producer
        env:
          CALLER_EVENT: ${{ github.event_name }}
          CALLER_REPOSITORY: ${{ github.event_name == 'pull_request' && github.event.pull_request.head.repo.full_name || github.repository }}
        run: |
          set -euo pipefail
          case "$CALLER_EVENT" in
            pull_request|push) ;;
            *) echo "::error::unsupported artifact producer event: $CALLER_EVENT"; exit 1 ;;
          esac
          if [ "$CALLER_REPOSITORY" != "layervai/qurl-integrations" ]; then
            echo "::error::the protected artifact producer does not accept a fork"
            exit 1
          fi

      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ github.event_name == 'pull_request' && github.event.pull_request.head.sha || github.sha }}
          persist-credentials: false

      - uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version-file: go.mod
          cache: true

      - name: Build exact sandbox customer artifacts
        env:
          CALLER_HEAD_SHA: ${{ github.event_name == 'pull_request' && github.event.pull_request.head.sha || github.sha }}
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          QURL_GO_SOURCE_SHA=$(scripts/resolve-sandbox-artifact-qurl-go-source.sh)
          scripts/build-sandbox-matched-cohort-pr-artifacts.sh \
            "$RUNNER_TEMP/sandbox-matched-cohort-pr-artifacts" \
            "$GITHUB_REPOSITORY" "$CALLER_HEAD_SHA" "$GITHUB_RUN_ID" "$GITHUB_RUN_ATTEMPT" \
            "$QURL_GO_SOURCE_SHA"

      - name: Upload exact sandbox customer binaries
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: sandbox-matched-cohort-binaries
          path: ${{ runner.temp }}/sandbox-matched-cohort-pr-artifacts/lifecycle-artifact/
          if-no-files-found: error
          include-hidden-files: false
          compression-level: 0
          retention-days: 30

      - name: Upload exact sandbox customer source receipt
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: sandbox-matched-cohort-source-receipt
          path: ${{ runner.temp }}/sandbox-matched-cohort-pr-artifacts/source-receipt-artifact/
          if-no-files-found: error
          include-hidden-files: false
          compression-level: 0
          retention-days: 30

  goreleaser-check:
    name: cli / goreleaser check
    runs-on: ubuntu-latest
    needs: changes
    if: needs.changes.outputs.cli == 'true'
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      # Config validation only — no Go toolchain, no build. The nightly
      # snapshot job (cli-nightly.yml) is what exercises the real build
      # pipeline; release-cli in release-please.yml is the publishing path
      # both of them protect.
      - name: Install GoReleaser
        uses: goreleaser/goreleaser-action@f06c13b6b1a9625abc9e6e439d9c05a8f2190e94 # v7.2.3
        with:
          version: "~> v2"
          install-only: true

      # `goreleaser check` exits 1 on an invalid config and 2 on a valid one
      # that uses deprecated properties. Both fail this gate on purpose: a
      # new upstream deprecation should redden CLI PRs here, where fixing it
      # is cheap, instead of surfacing first in release-cli. (An exit-2
      # tolerance lived here until .goreleaser.yml was migrated off its
      # deprecated keys; keep the gate strict.)
      - name: goreleaser check
        run: goreleaser check

  image-smoke:
    name: cli / image smoke
    runs-on: ubuntu-latest
    needs: changes
    if: needs.changes.outputs.cli == 'true'
    timeout-minutes: 40
    services:
      registry:
        image: registry@sha256:a3d8aaa63ed8681a604f1dea0aa03f100d5895b6a58ace528858a7b332415373 # registry:2
        ports:
          - 5000:5000
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - uses: docker/setup-qemu-action@96fe6ef7f33517b61c61be40b68a1882f3264fb8 # v4.2.0

      - uses: docker/setup-buildx-action@bb05f3f5519dd87d3ba754cc423b652a5edd6d2c # v4.2.0
        with:
          # The docker-container driver pushes the disposable candidate to
          # the job's registry service through the host network.
          driver-opts: network=host

      - uses: oras-project/setup-oras@1d808f7d7f6995cc68b7bf507bfe5c5446e1dc9d # v2.0.1
        with:
          version: 1.3.1

      - uses: sigstore/cosign-installer@6f9f17788090df1f26f669e9d70d6ae9567deba6 # v4.1.2
        with:
          cosign-release: 'v3.0.6'

      - name: Build two-platform scratch qurl image
        id: image
        uses: docker/build-push-action@f9f3042f7e2789586610d6e8b85c8f03e5195baf # v7.2.0
        with:
          context: .
          file: apps/cli/Dockerfile
          platforms: linux/amd64,linux/arm64
          push: true
          tags: localhost:5000/qurl:image-smoke
          build-args: VERSION=v0.0.0-image-smoke
          provenance: mode=max
          sbom: true

      - name: Verify runtime and signed BuildKit statement binding
        env:
          IMAGE_DIGEST: ${{ steps.image.outputs.digest }}
        run: |
          candidate="localhost:5000/qurl@${IMAGE_DIGEST}"
          for platform in linux/amd64 linux/arm64; do
            os="${platform%/*}"
            architecture="${platform#*/}"
            platform_digest="$(docker buildx imagetools inspect "$candidate" --raw | jq -er \
              --arg os "$os" --arg architecture "$architecture" '
                [.manifests[] | select(
                  .platform.os == $os and
                  .platform.architecture == $architecture
                )] |
                if length == 1 then .[0].digest
                else error("expected exactly one platform manifest")
                end
              ')"
            platform_candidate="localhost:5000/qurl@${platform_digest}"
            [ "$(docker run --rm --platform "$platform" "$platform_candidate" version | awk '{print $3}')" = 'v0.0.0-image-smoke' ]
          done
          docker buildx imagetools inspect "$candidate" --format '{{json .Image}}' | jq -e '
            length == 2 and
            all(to_entries[];
              .value.config.User == "65532:65532" and
              .value.config.Entrypoint == ["/usr/local/bin/qurl"]
            )
          ' >/dev/null

          attest_dir="$(mktemp -d)"
          QURL_EXPECTED_VCS_REVISION="$(git rev-parse HEAD)" \
          QURL_EXPECTED_VCS_SOURCE=https://github.com/layervai/qurl-integrations \
            scripts/extract-qurl-image-attestations.sh "$candidate" "$attest_dir" --plain-http
          cosign_password="$(openssl rand -hex 32)"
          export COSIGN_PASSWORD="$cosign_password"
          cosign generate-key-pair --output-key-prefix "$attest_dir/cosign"
          cosign sign --yes --allow-insecure-registry --key "$attest_dir/cosign.key" "$candidate"
          cosign attest --yes --allow-insecure-registry \
            --key "$attest_dir/cosign.key" \
            --type https://layerv.ai/attestations/qurl-image-buildkit-manifest/v1 \
            --predicate "$attest_dir/qurl-image-buildkit-manifest.json" \
            "$candidate"
          cosign verify --allow-insecure-registry --key "$attest_dir/cosign.pub" "$candidate" >/dev/null
          cosign verify-attestation --allow-insecure-registry \
            --key "$attest_dir/cosign.pub" \
            --type https://layerv.ai/attestations/qurl-image-buildkit-manifest/v1 \
            "$candidate" >/dev/null

  required:
    name: cli / required
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions: {}
    # Every CLI quality gate must be listed here; cli / required only
    # validates jobs in this needs set. display_names below is annotation-only.
    needs: [changes, lint, test, govulncheck, matrix, sandbox-customer-artifacts, goreleaser-check, image-smoke]
    if: always()
    steps:
      - name: Verify CLI CI result
        shell: bash
        env:
          CHANGES_RESULT: ${{ needs.changes.result }}
          CLI_CHANGED: ${{ needs.changes.outputs.cli }}
          NEEDS_JSON: ${{ toJSON(needs) }}
        run: |
          if [ "$CHANGES_RESULT" != "success" ]; then
            echo "::error::cli / detect changes concluded $CHANGES_RESULT"
            exit 1
          fi

          case "$CLI_CHANGED" in
            true)
              ;;
            false)
              echo "No CLI-impacting changes detected; CLI CI is not required."
              exit 0
              ;;
            *)
              echo "::error::cli / detect changes emitted unexpected cli output: ${CLI_CHANGED:-<empty>}"
              exit 1
              ;;
          esac

          display_name() {
            case "$1" in
              lint)
                echo "cli / lint"
                ;;
              test)
                echo "cli / test"
                ;;
              govulncheck)
                echo "cli / govulncheck"
                ;;
              matrix)
                echo "cli / matrix"
                ;;
              sandbox-customer-artifacts)
                echo "cli / sandbox matched-cohort artifacts"
                ;;
              goreleaser-check)
                echo "cli / goreleaser check"
                ;;
              image-smoke)
                echo "cli / image smoke"
                ;;
              *)
                echo "$1"
                ;;
            esac
          }

          failed=0
          needs_rows="$(jq -r 'to_entries[] | [.key, (.value.result // "")] | @tsv' <<<"$NEEDS_JSON")"
          while IFS=$'\t' read -r job result; do
            [ -n "$job" ] || continue
            if [ "$job" = "changes" ]; then
              # The change detector is checked above; it is not a CLI quality gate.
              continue
            fi
            if [ "$result" != "success" ]; then
              # CLI-impacting changes require every quality gate listed in
              # needs to run and pass; skipped/cancelled is a failed gate.
              echo "::error::$(display_name "$job") concluded ${result:-<empty>}"
              failed=1
            fi
          done <<<"$needs_rows"

          exit "$failed"
